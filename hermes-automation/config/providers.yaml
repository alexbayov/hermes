# Provider configuration — single source of truth for external services.
# Edit this file to change providers. Agent has NO choice — only what's here.

mail:
  provider: mailpit
  host: "127.0.0.1"
  smtp_port: 1025
  http_port: 8025
  domain: "test.local"
